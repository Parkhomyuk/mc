import { SomeDirectiveDirective } from './some-directive.directive';

describe('SomeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
